# 音声学習機能の実装記録

開始日: 2026-09-13。状態: **公開・定期運用を開始**。現在の公開本数、実行設定、確認結果と残件は[運用開始記録](audio-learning-launch.md)を参照してください。以下は導入の各段階の記録であり、「未実施」「確認中」はそれぞれの段階の観測を表します。

## 作業契約

- 目的: [確定した要件](../../plans/engineering/audio-learning.md)に基づき、記事単位の音声学習、ローカル自動制作、検査・公開・更新を実装します。
- 許可根拠: ユーザーの「OKです。実装に進んでください。」。既存契約の範囲と無料ソフトウェアを使う構成、公開まで自動化する要件を引き継ぎます。追加課金、有料 API、新規有料契約は許可されていません。
- 元 HEAD: `68b1b4f517498f5be575c326bb2c7fdc6fcdf1c3`。`origin/main` と同期を確認して `feat/audio-learning` を作成しました。
- リモート: `https://github.com/pero3dev/ai-agent-library.git`、PUBLIC、default branch は main。開始時に open PR はありません。既存の別 worktree は操作しません。
- 所有範囲: `website/` の音声 UI・同期・配信検証、`scripts/audio/`・`scripts/audio-run.mjs`・`scripts/audio-publish.mjs`・音声用 PowerShell、`automation/audio/`、対応する `tests/`、package のコマンド、必要な CI・配置契約、音声の要件・調査・本記録と各索引。
- 既存差分: 要件対話で作成した 4 文書だけです。本作業が引き継ぎ、他者の差分は戻しません。
- 検証: `npm ci` と `npm run check`、サイトの単体試験・静的ビルド・ブラウザー試験、音声制作・公開処理の失敗系試験、実 Claude 認証・ローカル合成・公開 URL の応答を実施できた範囲で確認します。
- 終了条件: 再生・制作・更新・検査と運用手順が実装され、要求への対応と検証・残件が追跡できること。iPhone 実機・契約認証・音声本番生成・GitHub 公開をモックだけで合格扱いにしません。

## 自律継続の作業契約

2026-09-13、ユーザーの「可能なところまですべての実装完了に向けて自律的に作業を進めていただきたいです。」を受け、残件の修正、既存 PR の検証・マージと Pages 公開、初回音声の公開、自動制作・公開タスクの導入まで継続します。制作から公開まで自動化する確定要件と既存の実装許可を引き継ぎ、同じ無料構成を使います。

- 再開 HEAD: `0d9af476aa99d41a1be2c3299238f76dd2dcb5db`。PR #35 は draft・OPEN、作業ツリーは clean、10 チェックの成功を確認しました。
- 所有範囲は元の作業契約と同じです。専用の運用 checkout とローカル定期タスクは今回の運用導入で追加します。他の worktree は変更しません。
- 音声制作は既存プロセスが継続しており、概要章の 2 記事が生成済み、3 記事目は制作中です。排他を守って既存の再開処理を引き継ぎます。
- 終了条件は、修正の独立レビュー・CI、公開サイトと実音声配信、定期タスクの登録・実行を可能な範囲で確認し、全記事の制作を継続できる状態にすることです。iPhone 実機と人間の聴き取り評価は未実施として残します。

## 初期観測

- サイトは Next.js/Nextra の静的出力、GitHub Pages 配信です。
- 2026-09-13 の `claude --version`: `2.1.246`。
- `claude auth status`: `loggedIn: false`、`authMethod: none`。台本の実生成にはユーザーによる Claude Pro のログインが必要です。API 課金へ切り替えません。
- PATH に ffmpeg / ffprobe は見つかりませんでした。ローカル合成・圧縮に使う実行ファイルは作業用ツール領域へ準備します。

## 自律継続での修正と確認

- 再生位置は画面を閉じる直前や通信エラー時の実メディア時計から保存します。読み込み前のシーク先、完了時の位置リセット、旧音声から届く遅延イベント、リスト上限の境界も確認しました。一覧には音声自体の公開日を表示します。
- 前提用語は既存の用語集と公開済み基礎記事の関連抜粋に根拠を限定し、作成者と検査者へ同じ資料を渡します。使用した抜粋をハッシュで照合し、無関係な用語集の更新や既存の合格済み音声で不要な再制作を起こしません。
- 公開待ち PR の main 更新は、原文と補助資料を再照合し、通常の merge commit と新 head の CI で追随します。他者のカタログ変更や不明な競合は保留します。
- 専用 linked worktree の共有状態パスを実環境で修正しました。main 同期後の依存更新も自動化し、取得に失敗した場合は制作を止めます。
- ローカルの `npm run check` は単体 416 件と各検査に成功。サイト単体は最終 61 件、音声 fixture の Chrome は 8 件、公開用静的ビルドは 223/223 ルートに成功しました。通信エラー時の位置保存の最終修正は追加の単体試験と提出後 CI で確認します。Windows 専用の音声試験も既存の Windows CI 対象に加えました。
- プレイヤー、制作・補助資料、Windows wrapper、公開復帰処理は担当外の独立レビューで指摘を解消しました。公開処理の最終 23 件は、実ローカル Git と GitHub モックで成功しました。push 後の metadata 更新失敗も、同じ head から再開して整合を確認します。
- 専用 checkout は Git 共通ディレクトリの `audio-learning/production-checkout`、branch は `chore/audio-production-main` です。初期依存の `npm ci` と空白を含む別の実 worktree のパス回帰試験が成功しました。定期タスクはまだ登録していません。
- 旧コードで制作中の skill-map は、最終台本に U+FFFD が 2 文字残ることを検出しました。最終検査では拒否されるため、所有する旧 producer の実行内容・開始時刻・子プロセス不在を確認して停止し、待機済み helper へ引き継ぎました。完成済み 2 記事と音声断片を保持し、最新コードで修正・再照合します。初回修正にも文字化けの指摘を渡す回帰を加え、制作 33 件に成功しました。
- 実公開前検査で learning-roadmap が保留になった原因は、helper の状態保存時に Windows が一時的な `EPERM` を返し、旧 READY が残ったことでした。元ファイルを保持した atomic rename を最大約 2.52 秒の有限再試行にし、制作 34 件と独立レビューに成功しました。helper 終了後に `--retry-held` で正規の検査証拠を更新します。
- 最初の提出後 Windows CI は、TEMP の短縮名 `RUNNER~1` と実パスの文字列比較で試験が失敗しました。共有状態の実パスを比較するよう試験を修正しました。実行コードのパス不具合とは区別し、修正後 head の CI で再確認します。
- Windows runner では `realpathSync` 後も短縮名が残ったため、両 worktree の保存先引数の一致と、保存先の親が期待する `.git` と同じファイルシステム ID であることを検査します。別の保存場所や誤ったパスの結合は引き続き拒否します。

## 公開環境での追加確認

- PR #35 は最終 10 チェックに成功し、`d84bb9179d4040e94a1f0df2679f451715b9e45d` として main に反映しました。main CI `34714897460` は Pages deploy を含めて成功しました。
- 公開後の URL 確認で、静的出力は `/audio.html` であり、末尾スラッシュ付き `/audio/` を公開検査に使うと 404 になる問題を検出しました。修正 branch `fix/audio-publication-route` はこの merge SHA を起点とし、公開処理・その回帰試験・本記録を所有します。既存の自律継続の許可で検証・PR・公開まで進めます。
- 初回音声の追加文字列レビューでは learning-roadmap の Copilot 利用と API 呼び出し経験・開発環境の混同を検出し、2 箇所を訂正して正規の再レビュー・再合成へ戻しました。skill-map は有限の自動修正後も指摘が残ったため保留し、合格した記事を先に公開します。READY の合格フラグを手で変更しません。

## 完成音声の修復要件への対応

REQ-037 の最終照合で、完成 MP3 の信号検査や結合時間の不一致に対する自動修復がなく、即座に保留する実装を検出しました。台本と発話単位の再試行だけでは要件全体を満たさないため、上限付きの修復を追加します。

- 作業 branch は `fix/audio-signal-recovery`、起点は公開検査修正 PR #36 の merge `f668c2328ec36949d44bd5177beae4d30b16781b` です。既存の自律継続の許可で、修復実装・試験・独立レビュー・PR・公開まで進めます。
- 所有範囲は `scripts/audio/synthesis.mjs`、`scripts/audio/publication.mjs`、対応する試験、本記録と運用開始記録・索引です。各担当の変更は別担当が独立レビューします。他者の作業と生成中の音声を戻しません。
- 失敗したパートの断片を検査し、不良断片の再生成または再エンコードを有限回試します。再生成後は時間・チャプター・ハッシュを再計算します。長い単一チャプターは発話境界で分割し、内容を省略しません。
- 終了条件は、正常音声の再利用、修復成功と上限到達、長尺分割を対象試験で確認し、全体検査・独立レビュー・最終 CI が成功することです。実 iPhone や人間の聴き取り評価は引き続き別の受入です。
- 本番カタログ PR #37 の進行中、個別 job の成功を確認しても workflow 全体が実行中だと `Check workflow identity mismatch` になる問題を観測しました。head・実行元・check suite の厳密な照合は維持し、実行中は待機、終了後の失敗は保留、全体成功後だけマージへ進むよう修正します。
- 修正後の `npm run check` は 431 件すべて成功し、skip はありません。合成の新規 9 件には実 FFmpeg での不良断片の検出・修復を含みます。公開処理 28 件と合成の担当外レビューはいずれも指摘を残していません。
- 初回カタログ PR #37 は 10 チェックを通過し、`983a0e00def0fa4bb82da5be18c80ac9f0d25188` として main に反映しました。修復 branch もこの main へ fast-forward し、他者の変更を保持しています。Pages deploy は確認中です。
- Windows の `AI Agent Library - Audio Learning` を登録し、2026-09-13 05:05:47 JST に自動起動しました。専用 checkout から全 199 記事を認識し、既存の完成 2 本・保留 1 本を飛ばして Agent ループの記事を制作中です。登録と実起動を確認した結果であり、この定期実行の全工程が完了したという意味ではありません。

## 作業段階

1. 共通の音声データ契約、作業状態、カタログ連携。
2. サイトの常駐プレイヤー・記事の入口・一覧と再生リスト。
3. Claude 台本・独立確認、ローカル音声合成、再開可能な制作と品質検査。
4. 音声成果物の公開・カタログ更新・定期実行と課金防止。
5. 統合検証、実行できた音声の制作・配信確認、導入手順と残件の保存。

## 分担と接続

- プレイヤー担当: `website/components/audio/`、`website/lib/audio-player.mjs`、`website/app/audio/`、layout・MDX wrapper・CSS と再生試験。
- 制作担当: `scripts/audio/` の制作モジュール、`scripts/audio-run.mjs`、`automation/audio/config.json` と制作試験。
- 公開担当: `scripts/audio/publication.mjs`、`scripts/audio-publish.mjs`、Windows の起動・定期実行と公開試験。
- 統合担当: カタログの正本・同期・検査、ツール導入、package・CI、実行観測と運用記録。担当外の差分を戻さず、共通スキーマを共有して並行作業しました。

## 実装中の観測

- `npm ci` は root と website の両方で成功し、どちらも audit の脆弱性 0 件でした。
- 公式 GitHub Releases の SHA-256 と照合して VOICEVOX Nemo 0.24.0 Windows CPU を `.git/harness-tools/audio-learning/` に展開しました。FFmpeg 9.0.1 も配布元の SHA-256 と照合しました。導入スクリプトは global PATH・Python・レジストリを変更しません。
- 実エンジンを `127.0.0.1:50121`、CPU 2 スレッド、低優先度で起動し、`/speakers` で女声1 (`10005`)・男声1 (`10001`) を確認しました。設定するクレジットは「VOICEVOX Nemo（女声1・男声1）」です。
- 初回のカタログは空です。試験用の音声を公開済みの教材として見せません。サイト同期では現在の記事ハッシュと比較し、旧版を表示対象として保持、非公開・削除された記事は音声一覧から除外します。
- テスト用カタログを使うビルドは `/__audio-test` 専用です。CI の専用 job は Pages artifact をアップロードせず、通常公開ビルドと分離します。
- Claude のログインに加え、アカウントの Extra usage が無効であることをユーザーに確認中です。CLI の認証情報だけで追加使用の設定まで確認できるとは扱いません。

## 検証の進捗

- root の `npm run check`: 単体 392 件と Markdown・記事構造・リンク・TODO・ハーネス・Git 形式設定の検査が成功しました。その後の独立レビュー修正は音声の対象試験を再実行しています。
- website の `npm test`: 単体 51 件が成功しました。シークの再試行不具合はこの後の独立レビューで見つかり、追加修正の対象です。
- 実合成: 教材として公開しない二人の会話を 30.773333 秒の MP3 に生成し、2 チャプター、無音約 1.095 秒を確認しました。
- ユーザーが Extra usage 無効を確認しました。Claude Code の認証も再確認で有効になり、`audio:doctor` の全項目が成功しました。確認記録は git 管理外に保存しています。
- `00-overview` の全 3 記事を実制作中です。最初の学習ロードマップは 9 チャプターの台本を生成し、原文との独立照合へ進みました。台本中の U+FFFD は 0 件でした。
- 独立レビューでは、シーク失敗後の再試行、UTF-8 の分割受信、異常時のロック解放、古い検査情報の再利用、公開直前のファイル変更と待機中 PR の競合を確認・修正しています。

## 最初の実装完了時点の検証

| 対象 | 結果 |
| --- | --- |
| root 全体 | `npm run check` 成功、単体 401 件。Markdown・記事・リンク・TODO・ハーネス・Git 設定の検査も成功 |
| サイト単体 | `npm --prefix website test` 成功、52 件 |
| 音声 fixture | 最初の Chrome 7 件が成功。独立レビュー後のシーク・モバイル・報告リンクの関連 3 件も成功 |
| 公開用ビルド | `STATIC_EXPORT=1`、`/ai-agent-library` で成功。230 HTML、期待ルート 223/223、16 セクションを確認。`test_only: false`、公開カタログは空 |
| 公開用ブラウザー試験 | インストール済み Chrome で 87 件成功、fixture 専用の 5 件は意図した skip |
| Workflow | 固定ハッシュで取得した actionlint 1.7.12 の Windows 版で成功 |
| PowerShell | Windows PowerShell 5 でタスク XML と wrapper の dry-run を実行する試験が成功。タスクは未登録 |
| 実音声エンジン | Nemo 0.24.0 + FFmpeg 9.0.1 の実合成・圧縮・信号検査が成功 |
| 実 Claude | 契約認証と台本生成・独立内容照合を確認。図の依存関係の説明誤りを検出し、修正中 |

全ブラウザー試験の最初の実行では、既存 Mermaid 試験が一度見つけた SVG のサイズを取得する間に要素が切り替わり、`boundingBox()` が null になって 1 件失敗しました。サイズの条件自体を待つ検査に直し、該当例 3 回と全体を再実行して成功しました。記事や Mermaid の描画コードは変更していません。

ログ・スクリーンショット・実音声は Git 共通ディレクトリの `audio-learning/` に保持しています。試験ログは `root-check-final.log`、`website-unit-final.log`、`production-build.log`、`production-browser-final.log`。画面は `evidence/audio-mobile.png` と `evidence/audio-desktop.png`。生成中の台本・音声は `jobs/`、教材ではない合成接続試験は `smoke/` にあります。

## 初回提出時点の残件と次の操作

以下は初回提出時点の状態です。公開、定期タスク、古くなったPRの復帰処理は後続の実装・確認へ進み、現在の状態は[運用開始記録](audio-learning-launch.md)にまとめています。

- `00-overview` 全 3 記事の実制作を継続中です。最初の「AI Agent 学習ロードマップ」は、台本の修正・再照合と実合成を終え、音声信号検査にも合格しました。残りの記事は制作を継続しています。
- 最初に起動した制作プロセスは改良前の READY 形式を読み込み済みです。終了後、同じ章の `--run` を再実行し、承認済み台本・音声断片を再利用して最新の音声検査証拠を作ります。これを行う一回限りの非表示プロセスを起動しました。状態は `.git/audio-learning/initial-batch-finish.json` と同接頭辞の stdout/stderr ログに残します。古い READY を手で合格扱いにしません。
- main への反映、GitHub Releases の実配信、カタログ PR、Pages の公開、定期タスク登録は未実施です。公開用カタログは完成した音声だけを掲載します。
- iPhone 実機受入と音声の聴き取りやすさは、[運用手順](../../../automation/audio/README.md)の受入表で確認します。現在の自動試験で代用したとは扱いません。
- 自動公開中に main が進んでカタログ PR の base が古くなった場合は、`HELD_BASE_CHANGED` で保留します。原文を再照合して PR を更新する運用判断が必要です。競合を自動で無視する実装は入れていません。

## 提出と実環境での追加修正

- [ドラフト PR #35](https://github.com/pero3dev/ai-agent-library/pull/35)に実装を提出しました。初回 HEAD `436723e` の既存 9 必須チェックと音声ブラウザー検査がすべて成功しました。PR の deploy は意図した skip です。
- この PC で `Get-Command pwsh` が複数の実行ファイルを返すことを観測しました。定期タスクには現在の `$PSHOME/pwsh.exe` を指定し、Node の解決も単一パスへ限定しました。Windows PowerShell 5 と PowerShell 7 の XML・dry-run を含む公開処理 15 試験が成功しています。
- 現行 HEAD の CI は PR のチェック欄で確認します。ドラフト PR の提出を main への反映・公開完了と扱いません。

## 修正前の初回教材音声

「AI Agent 学習ロードマップ」を 9 チャプター、1,570.037347 秒(約 26 分 10 秒)、12,560,761 bytes の MP3 にしました。原文との独立照合は修正後に合格、最終台本・レビューに U+FFFD はなく、実音声の長さ・無音・結合検査も合格しました。音声 SHA-256 は `e01c418b1abf97e7f2e56a2705bcf2e052ee1f6a1ea55979eb37b330715d71b1` です。

試聴用コピーは Git 共通ディレクトリの `audio-learning/previews/learning-roadmap.mp3`、対応する情報は同名の JSON に保存しました。音声は VOICEVOX Nemo（女声1・男声1）です。人間が聴いた品質評価、iPhone 実機、公開 URL の試験ではありません。GitHub へ音声本体はアップロードしていません。

後続の内容確認で前提条件の説明を訂正し、再レビュー・再合成した新版へ試聴用コピーも更新しました。上記の長さ・ハッシュは旧版の観測であり、現在の公開音声は[運用開始記録](audio-learning-launch.md)の約26分16秒の版です。
