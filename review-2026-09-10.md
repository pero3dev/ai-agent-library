# プロジェクトレビュー記録(2026-09-10)

対象: `ae86cc3aff60f6571e1b201edaa31d1219789954` の作業ツリー。レビュー開始時の Git 差分はありませんでした。

**総合判定: 要修正です。** 記事構造・相対リンク・静的サイトの生成は整っています。一方、次回 CI を止める依存関係、承認順序を逆に示す図、実行可能な MDX を通す検査漏れ、誤った出力を成功とするサンプル、保証範囲を過大に説明する記事が見つかりました。

この記録は監査結果です。記事・サンプル・依存・公開ステータスの修正やデプロイは行っていません。P1 は先に対処すべき問題、P2 は通常の修正対象、改善提案は別枠としています。優先度はこのプロジェクトへの影響であり、依存パッケージの CVSS 分類とは別です。

確定指摘は **26 件(P1: 2 件、P2: 24 件)**、改善提案は 10 件です。再現結果は [サンプルの実行記録](review-evidence/2026-09-10/examples-audit.json) と [サイトの検証記録](review-evidence/2026-09-10/website-audit.txt) に保存しています。

## 検証範囲と結果

| 対象 | 実施内容 | 結果 |
| --- | --- | --- |
| 全 199 記事 + セクション README 16 件 | `node scripts/validate-docs.mjs --all` | 成功: 215 files |
| docs・examples・ルート README・GLOSSARY | `node scripts/check-links.mjs` | 成功: 224 files / 3,995 links |
| リポジトリの Markdown | `npm run lint:md` | 成功 |
| TODO | `node scripts/todo-report.mjs` | 184 件 / 121 ファイル。2026-07: 118 件、2026-08: 66 件 |
| 記事ステータス | front matter の全件集計 | 199 件すべて published |
| サイト依存 | `npm audit --json`(website) | 失敗: critical 1 / high 2 / moderate 2 |
| サイト生成 | `STATIC_EXPORT=1`、`NEXT_PUBLIC_BASE_PATH=/ai-agent-library`、公開 SITE_URL を設定して `npm run build:clean` | 成功。期待ルート 222 / 生成 222、Next の生成表示 229 pages |
| 生成 HTML | 228 HTML の 117,071 個のリンクを静的照合 | 未生成パス 0 / basePath 逸脱 0 / 不在アンカー 6(F22) |
| 検索 | 生成された Pagefind WASM を Node から実行 | 222 ページを索引化。「ツール使用」で 2 件を取得 |
| サンプル | Python 3.11.3 で 5 件の既定モック実行、tool-use の構文確認 | 成功。境界入力では F13〜F17 を再現 |
| MCP | 固定版 `mcp==1.28.1`、実 stdio クライアント | initialize / list_tools / call_tool 成功。業務エラーの誤分類を再現 |
| SDK | 固定版 `anthropic==0.116.0` の導入・応答スタブ | 導入成功。打ち切り応答の成功扱いを再現。実 API 呼び出しなし |
| Codex フック | 公式の apply_patch イベント相当の入力を既存スクリプトへ渡す | 生成物の変更を許可してしまう(F04) |
| 内容 | 全 16 セクションを対象に主張の横断抽出・重点精読、関連章との照合、選択した一次資料の確認 | 下記指摘 |

環境は Windows / PowerShell、Node.js 24.16.0、Python 3.11.3 です。既定の `python` は 3.10.1 だったため、サンプルの判定には要件を満たす 3.11.3 の結果を使用しました。

意味的レビューはリスクに基づく重点レビューです。全 199 記事の全主張を一次資料で再検証したという意味ではありません。全外部 URL の疎通検査、実 LLM API の品質・費用・レイテンシ、CI の Ubuntu / Node 22 での再実行、actionlint、公開先のデプロイ状態は未検証です。CUA の接続一覧が `apps=[] / browsers=[]` だったため、実ブラウザー、キーボード操作、スクリーンリーダーによる受入確認も未実施です。

分担レビューで全文精読を記録した記事は 73 件です。主担当は下表の全範囲の抽出結果・指摘を統合し、08・10・11 章を中心に保証表現、設計判断、モデル情報、一次資料を追加確認しました。「指摘なし」は全主張の正しさを保証するものではありません。

| 範囲 | 記事数 | 分担レビューの深さ |
| --- | ---: | --- |
| 00・01・07 章 | 22 | 22 件全文精読。各 README と関連する実装・安全性記事も照合 |
| 02〜06 章 | 90 | 42 件全文精読、48 件は横断抽出。04 評価章は全 12 件を精読 |
| 09・12〜15 章 | 44 | 9 件全文精読、残りは規制・安全性・保証表現・参考資料を中心に確認 |
| 08・10・11 章 | 43 | 主担当による横断抽出・重点精読・一次資料との照合 |

02〜06 章の全文精読対象は、指摘対象 13 記事に加え、`agent-api-design`、`context-engineering`、`human-in-the-loop`、`orchestration-patterns`、`code-execution-sandboxes`、`long-term-memory-implementation`、`streaming-and-agent-ux`、`structured-output`、`tool-definition-design`、`vector-databases`、`agent-benchmarks-landscape`、`agent-evaluation-basics`、`confidence-and-calibration`、`evaluation-datasets`、`evaluation-environments`、`fairness-and-bias-evaluation`、`japanese-quality-evaluation`、`online-evaluation-and-ab-testing`、`regression-testing`、`trajectory-evaluation`、`cost-management`、`llm-gateway`、`observability-and-tracing`、`advanced-attack-patterns`、`agent-identity-and-auth`、`guardrails`、`prompt-injection`、`threat-model-overview`、`tool-permissions-and-sandboxing` です。

09・12〜15 章の全文精読対象は、`roi-and-business-case`、`usecase-discovery`、`poc-to-production`、`ai-procurement`、`ai-usage-policy`、`spreadsheet-agents`、`data-analysis-agents`、`personal-assistant-design`、`verifying-ai-outputs` です。GLOSSARY の全用語について、本文との意味的な同期を網羅確認したわけではありません。

## 優先して修正する問題

### F01 — P1: 依存監査で次回の CI とデプロイが停止します

根拠: [website/package-lock.json](website/package-lock.json) L4983、L5985、L1906 / [.github/workflows/ci.yml](.github/workflows/ci.yml) L102〜104、L122。

lock の実値は Next.js 16.3.1、sharp 0.35.3、xmldom 0.9.10 です。監査は critical 1 / high 2 / moderate 2 を返し、CI が必須にしている `npm audit --audit-level=high` を通過できません。ローカルの build 単体の成功と、CI 全体の成功を分ける必要があります。

修正方針: 修正版へ依存と lock を更新し、監査と basePath 付き静的ビルドを再実行します。Next 16.3.3、sharp 0.35.4 に関する修正情報を確認しています。[Next advisory 1](https://github.com/advisories/GHSA-p293-qw3h-jr36)、[Next advisory 2](https://github.com/advisories/GHSA-2xp9-vwfh-vxw4)、[sharp advisory](https://github.com/advisories/GHSA-rgj7-g3m4-5g8c)(確認日: 2026-09-10)。静的 GitHub Pages に Next のサーバー RCE がそのまま成立するとは判定していません。

### F02 — P1: 操作を実行してから人間に承認を求める図になっています

根拠: [computer-use-implementation.md](docs/03-implementation/computer-use-implementation.md) L35〜40。本文 L83 とチェックリスト L113 は「操作の前に承認」と説明しています。

Mermaid は「モデルが決定 → クリック等を実行 → 不可逆操作か判定 → 人間承認」の順です。送信・削除後の承認では副作用を止められず、そのまま実装すれば安全装置が機能しません。

修正方針: 「操作案の決定 → 不可逆性・権限の判定 → 必要時に承認 → 承認された操作を実行」の順にします。同記事 L107 の「API・MCP で置換できる区間を最小化」も本文の推奨と逆で、「GUI に残す区間を最小化」へ修正します。

## パイプラインとフック

### F03 — P2: MDX の属性内の式が安全性検査を通過します

根拠: [sync-content.mjs](website/scripts/sync-content.mjs) L115〜120 / [SECURITY.md](SECURITY.md) L29〜30。

`assertSafeMdx` は許可された JSX 名と children を調べますが、属性内の式を走査しません。無害な入力 `<GlossaryTerm href={1+1}>probe</GlossaryTerm>` が同じ変換・検査処理を `errors=[]` で通過し、MDX 評価後の `props.href` は `2` になりました。「本文から式が混入したら失敗させる」という宣言と異なります。

修正方針: 属性値の `mdxJsxAttributeValueExpression` と spread を拒否し、コンポーネントごとの許可属性・値の型も確認します。記事だけを非実行コンテンツとしてレビューする運用での防御欠落です。任意の repository code を変更できる者に対する完全な隔離を主張するものではありません。

### F04 — P2: Codex フックが apply_patch の入力を処理しません

根拠: [.codex/hooks/guard-generated.mjs](.codex/hooks/guard-generated.mjs) L38〜42 / [.codex/hooks/validate-doc.mjs](.codex/hooks/validate-doc.mjs) L26〜30。

両フックは `tool_input.file_path` だけを読み、存在しなければ成功終了します。公式のパッチ走査例は `tool_input.command` を使用します。[OpenAI Hooks](https://learn.chatgpt.com/docs/hooks)(確認日: 2026-09-10)。この形で `website/content/probe.mdx` への追加パッチを guard に渡すと、無出力・exit 0 でした。同じ対象を `file_path` で渡すと exit 2 になるため、入力形式の不一致を切り分けています。

修正方針: apply_patch の全対象パス(追加・更新・削除・移動元/移動先)を取得し、事前ガードと事後検証に渡します。実 Agent での発火も追加確認が必要です。さらに [.codex/hooks.json](.codex/hooks.json) L10、L24 の `c:\dev\ai-agent-library` 固定パスを、チェックアウト場所に依存しない指定へ変えます。

## セキュリティ・設計・評価の説明

### F05 — P2: 画像プロキシをデータ漏えい防止と同列に扱っています

根拠: [data-exfiltration.md](docs/06-security/data-exfiltration.md) L43、L83 / [メール漏えい事例](docs/07-case-studies/case-study-email-assistant-incident.md) L84。

機微情報が URL の path/query に埋め込まれた場合、プロキシがその URL を上流へ取得すれば、機微情報は攻撃者へ届きます。閲覧者 IP の秘匿と、URL に載った情報の送信防止は異なります。チェックリストは単なるプロキシ化だけでも合格に読めます。

修正方針: 外部 fetch の禁止、画像にも適用する送信先制限、URL 内容とリダイレクトの検証を明示します。プロキシ化だけで漏えい経路を閉じたとしないよう、事例側も同期します。

### F06 — P2: 「実行前チェック」だけでも二重実行を防げる説明です

根拠: [async-and-durable-agents.md](docs/02-architecture/async-and-durable-agents.md) L99〜100。関連: [error-handling-and-retries.md](docs/02-architecture/error-handling-and-retries.md) L45、[deployment-and-scaling.md](docs/05-operations/deployment-and-scaling.md) L69。

2 ワーカーが同時に未実行と確認すれば両方が送信できます。送信成功後・完了記録前に落ちた場合も、送信側の事前チェックでは再送を防げません。記事はクラッシュ窓には触れますが、受信側の冪等キー受理と一般の事前チェックを同列に置いています。

修正方針: 受信側の冪等キー契約、同じ DB 内なら記録と更新の原子性、外部 API が非対応なら結果照合と自動再送を止める条件を分けて説明します。[AWS Builders Library](https://aws.amazon.com/builders-library/making-retries-safe-with-idempotent-APIs/)もトークン記録と更新の原子性を説明しています(確認日: 2026-09-10)。

### F07 — P2: 差分プライバシーの保証範囲を過大に説明しています

根拠: [privacy-enhancing-technologies.md](docs/06-security/privacy-enhancing-technologies.md) L40、L46、L54。

「個人の影響が残らない」「集計から個人が逆算されない保証」は強すぎます。差分プライバシーは、保護単位が異なる隣接データセット間の出力確率の差を制約します。集団の傾向から個人の属性を推論することまで不可能にはしません。[NIST SP 800-226](https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.800-226.pdf)の保証・非保証の区別が一次根拠です(確認日: 2026-09-10)。

修正方針: ε・δ、保護単位、繰り返し公開によるプライバシー予算の消費を、適用判断に必要な範囲で補います。「復元・推論が一切できない」という保証に読める表現を取り除きます。

### F08 — P2: API キー分割で容量を物理分離できると断定しています

根拠: [multi-tenancy-and-isolation.md](docs/02-architecture/multi-tenancy-and-isolation.md) L98、L117。

キーやプロジェクトを分けるだけでは、組織やモデル群の共有上限を分離できない場合があります。OpenAI は組織・プロジェクト単位の制限と、モデル群の共有枠を明記しています。[OpenAI Rate limits](https://developers.openai.com/api/docs/guides/rate-limits)(確認日: 2026-09-10)。したがって、専用キーから「暴走の波及を構造的に断てる」とは導けません。

修正方針: 認証・計測の分離と、独立した容量保証を区別し、共有上限に合わせた全体制御・テナント別配分を残します。

### F09 — P2: 語順の正規化を低リスクなキャッシュキーに含めています

根拠: [semantic-caching.md](docs/05-operations/semantic-caching.md) L46。

「大文字小文字・空白・語順などを正規化して一致」の誤ヒットリスクを低としています。しかし `A pays B` と `B pays A` は同じ単語集合で意味が逆です。語順を落とすキーでは別の質問へ同じ応答を返します。

修正方針: 自然言語で意味を失う変換を安全な正規化例から外し、用途ごとに意味保存が保証できる変換だけを採用します。大小文字や空白も、コード・識別子等では意味を変えうる点を添えます。

### F10 — P2: judge を調整したデータで、そのまま採用判定しています

根拠: [llm-as-a-judge.md](docs/04-evaluation/llm-as-a-judge.md) L64〜69 / [evaluation-datasets.md](docs/04-evaluation/evaluation-datasets.md) L90、L107。

20〜50 件の不一致を読んでプロンプトを修正し、その一致率が目安に達したら導入する手順です。調整に使ったケースへの適合を運用品質として判定できてしまいます。別記事は調整と判定に同じセットを使うことをアンチパターンとしています。

修正方針: judge の開発用と未見の採用判定用を分け、判定用を調整に使ったら更新します。安全上重要な誤合格率も独立して確認します。

### F11 — P2: τ-bench のユーザー役と評価対象 Agent の制御を取り違えています

根拠: [user-simulator-design.md](docs/04-evaluation/user-simulator-design.md) L58、L112。

引用論文のユーザーシミュレーションは、初期指示と会話履歴を用いる LLM です。ドメイン API・DB を操作するのは評価対象 Agent であり、ユーザー役はそのツール通信を見ません。「ユーザー役をドメイン API と状態で縛る」ことの実例としては不正確です。[τ-bench 論文 Section 3](https://arxiv.org/html/2406.12045v1)(確認日: 2026-09-10)。

修正方針: 初期シナリオ・会話によるユーザー役の誘導、Agent のツール環境、最終 DB 状態による採点を分けます。シミュレータ発話を構造的に制約したい場合の検証処理は別に説明します。

### F12 — P2: 埋め込みの入力超過を一律に「切り捨て」としています

根拠: [embeddings.md](docs/03-implementation/embeddings.md) L54、L83。

入力上限を超えると自動的に末尾だけ失うという一般仕様はありません。利用する API・ライブラリによって拒否・明示的切り詰め等が異なります。OpenAI の契約も入力上限を超えないことを要求しています。[Create embeddings](https://developers.openai.com/api/reference/resources/embeddings/methods/create)(確認日: 2026-09-10)。暗黙の切り捨てを前提にすると文書取り込み自体が失敗します。

修正方針: 事前のトークン計数と分割を基本とし、超過時挙動・切り詰め設定・欠落検知を採用先で確認する手順にします。実 API への上限超過リクエストは送っていません。

## 実行サンプル

### F13 — P2: 否定された正解ラベルも正解として採点します

根拠: [eval_harness.py](examples/python/evaluation-harness/eval_harness.py) L47〜51。

ラベルの部分一致で正規化するため、「請求ではありません。その他です」を「請求」と扱います。全 5 ケースで `expected + 'ではありません。判定できません。'` を返す偽分類器が合格率 100% になることを再現しました。コメントの「単純化」が評価の目的を損なっています。

修正方針: 空白除去後の許容ラベル完全一致、またはスキーマ検証済み出力を採点します。不正形式は失敗にし、生の応答も失敗解析用に保持します。

### F14 — P2: 生成上限による途中出力を成功として返します

根拠: [tool-use/main.py](examples/python/tool-use/main.py) L98〜102 / [agent-loop.md](docs/01-concepts/agent-loop.md) L51、L98〜99。

`tool_use` 以外をすべて最終応答とし、`max_tokens` や `model_context_window_exceeded` でも警告だけでテキストを返します。両停止理由で応答スタブを使い、途中テキストが通常 return されることを再現しました。公式もこれらを打ち切りとして区別しています。[Anthropic stop reasons](https://platform.claude.com/docs/en/build-with-claude/handling-stop-reasons)(確認日: 2026-09-10)。

修正方針: 正常終了、上限到達、継続可能な停止、拒否等を明示的に分岐し、不完全な結果を成功として返さないようにします。概念記事の「ツール要求なし = 正常完了」という説明にも同じ条件を反映します。

### F15 — P2: MCP の業務エラーが isError=false になります

根拠: [mcp_server.py](examples/python/mcp-server/mcp_server.py) L57〜58。

負の金額をエラー文字列として return するため、実 stdio で `submit_expense(-1, 'audit')` が `isError=False` になりました。型違いの入力は SDK が `isError=True` にするため、業務検証の return 経路が原因です。クライアントの失敗判定・補正が働かなくなります。[MCP Tool error handling](https://modelcontextprotocol.io/specification/2025-11-25/server/tools#error-handling)(確認日: 2026-09-10)。

修正方針: SDK の ToolError、または `CallToolResult(isError=True)` を使い、通常結果とエラーをプロトコル上でも区別します。

### F16 — P2: マルチエージェントの欠損・空計画・重複を成功扱いします

根拠: [multi_agent.py](examples/python/multi-agent/multi_agent.py) L82〜86。

取得できなかった都市を黙って除外して平均を出します。計画が「東京・大阪・北海道札幌市」なら、札幌を取得できないまま 29.0 度 / 2 都市と表示します。空計画は 0.0 度 / 0 都市、東京の重複は 27.5 度 / 4 都市になります。

修正方針: 計画の対象と重複を検証し、全件成功・部分結果・失敗を分けます。0 件は平均を計算できない状態として返します。

### F17 — P2: 無効な年月を「該当データなし」と返します

根拠: [tool-use/main.py](examples/python/tool-use/main.py) L73〜78。

7 文字と 5 文字目のハイフンしか確認しないため、`abcd-ef`、`2026-13`、`2026-00` がすべて件数 0 の通常応答になります。モデルが誤引数を補正する機会を失います。

修正方針: 年月の桁・数値・月範囲を検証し、入力不正と正しい検索の 0 件を分けます。

## 基礎・事例・ビジネス・導線

### F18 — P2: 温度 0 を決定論の十分条件として推奨しています

根拠: [inference-internals.md](docs/11-llm-internals/inference-internals.md) L66、L126 / [how-llms-generate-text.md](docs/10-llm-foundations/how-llms-generate-text.md) L67、L94。

発展章は「決定論が要るなら T=0」と推奨しますが、基礎章は温度 0 でも実装上の揺らぎがあり完全一致を保証しないと正しく説明しています。回帰テストを完全一致に依存させる誤解につながります。

修正方針: 固定されたロジット上の貪欲選択と、実システム全体の再現性を区別します。環境・モデル版の固定、意味や成功条件による評価も必要とします。

### F19 — P2: ROI を純便益額として定義しています

根拠: [roi-and-business-case.md](docs/09-business/roi-and-business-case.md) L28〜30。

「ROI 自体は効果 − コストという引き算」は純便益額の説明です。投資規模の異なる案件を比較する率になっていません。一般的な ROI は純利益を投資額で割った百分率です。[豪州政府 Key financial terms](https://business.gov.au/finance/financial-tools-and-templates/key-financial-terms)(確認日: 2026-09-10)。

修正方針: 評価期間・便益・費用・分母を定義し、純便益と ROI を分けて例示します。レビュー時間を純削減時間から引く場合、費用側でも同じ人件費を再度控除しないよう計上方法を揃えます。

### F20 — P2: LLM に数値予測をさせると精度が下がると一律に断定しています

根拠: [time-series-and-forecasting.md](docs/13-domain-agents/time-series-and-forecasting.md) L28〜47、L115。

専用手法を既定にする方針は妥当ですが、「置き換えると精度が下がります」は条件のない実証主張です。数値系列をテキスト化した LLM の予測が、検証条件によって専用手法と同等以上になる原論文があります。[Large Language Models Are Zero-Shot Time Series Forecasters](https://arxiv.org/abs/2310.07820)(確認日: 2026-09-10)。これは任意の業務で LLM を推奨する根拠ではなく、一律の劣化断定に対する反例です。

修正方針: 汎用チャットへの丸投げ、時系列向け手法、専用基盤モデルを区別し、時間順の検証・予測時点の情報制約・専用ベースラインとの比較で決める説明にします。執筆テンプレートだけの参考資料も置き換えます。

### F21 — P2: 採用規制の確認先に、採用を扱わない記事を案内しています

根拠: [hr-and-recruitment-ai.md](docs/13-domain-agents/hr-and-recruitment-ai.md) L68、L107、L114 / [industry-regulations-map.md](docs/09-business/industry-regulations-map.md) の金融・医療・公共・教育の各節。

参照先を「雇用・採用を含む分野別規制の確認先」と説明していますが、その記事に採用・雇用の確認先はありません。リンクは存在しても、「どの一次情報へ行けばよいか」という記事の目的を満たしません。

修正方針: 人事記事に実際の確認先と適用を確認する観点を置くか、参照先に対象節を追加します。免責や「法務へ確認」という記述だけで、所在の欠落を補ったことにしないようにします。

### F22 — P2: カスタムページの「本文へスキップ」のリンク先がありません

根拠: [app/page.jsx](website/app/page.jsx) L16、[glossary/page.jsx](website/app/glossary/page.jsx) L11、[roadmap/page.jsx](website/app/roadmap/page.jsx) L12、[tags/page.jsx](website/app/tags/page.jsx) L13。

共通レイアウトの `#nextra-skip-nav` に対応する id が各 main にありません。生成 HTML 全体でこの 4 ページと 404 / _not-found の計 6 件に不在アンカーを確認しました。キーボード利用者がナビゲーションを飛ばして本文へ進めません。

修正方針: 主内容へ対応 id と必要なフォーカス設計を追加し、キーボードで確認します。アンカー不在は静的に確認済みですが、実ブラウザーのフォーカス移動は未検証です。

### F23 — P2: API 操作にはインジェクションのリスクがなかったと説明しています

根拠: [computer-use-and-multimodal-agents.md](docs/01-concepts/computer-use-and-multimodal-agents.md) L51、L75。

「API なら存在しなかったリスク(誤クリック・インジェクション)」は、UI 座標の脆さとインジェクションを混同しています。[prompt-injection.md](docs/06-security/prompt-injection.md) L41 はツール結果を攻撃面に含め、メール漏えい事例も API による取得と応答表示で成立しています。構文上妥当な誤送信・誤削除は API でも成功します。

修正方針: API が減らすのは画面変化・座標等の不確実性であり、非信頼のツール結果・意味的な誤操作・認可の問題は残るとします。

### F24 — P2: 画像経由の漏えいを、読み取りモードで封じ込めた事例です

根拠: [case-study-email-assistant-incident.md](docs/07-case-studies/case-study-email-assistant-incident.md) L74、L92。

社内検索を止め、メール要約だけを続けても、履歴や要約対象メールの非公開情報を外部画像 URL に載せる経路は残ります。事例が特定した実際の送信経路と、被害を最小化したという縮退手段が対応していません。

修正方針: 最初に外部画像取得・外部通信と進行中セッションを止め、その後に安全性を確認した要約へ縮退します。読み取りモードを汎用的な漏えい防止と説明しないようにします。

### F25 — P2: 複数検索や計算だけを理由に Agentic RAG を推奨しています

根拠: [rag-vs-agent.md](docs/01-concepts/rag-vs-agent.md) L49、L58〜59 / [rag-implementation-patterns.md](docs/03-implementation/rag-implementation-patterns.md) L95。

選定表はクエリの言い換え・複数ソース横断・検索結果を使う計算を Agentic RAG の選定理由にしています。しかし、事前定義したワークフローでも実現でき、実装編自身も固定 RAG でのクエリ書き換えを推奨しています。[Anthropic の設計原典](https://www.anthropic.com/engineering/building-effective-agents)も、事前定義した経路とモデルによる動的な経路選択を区別しています(確認日: 2026-09-10)。この表では、不要な自律性・費用・検証範囲を増やす選択につながります。

修正方針: 単純な固定 RAG、複数ステップの固定ワークフロー、モデルが検索・実行経路を選ぶ構成を分けます。操作数ではなく、入力や途中結果に応じた自律的な経路選択が必要かを選定軸にします。

### F26 — P2: Web 閲覧を外せば非信頼コンテンツを除去できる例になっています

根拠: [data-exfiltration.md](docs/06-security/data-exfiltration.md) L52〜57 / [prompt-injection.md](docs/06-security/prompt-injection.md) L45、L81。

「信頼できないコンテンツ」を外す例として、社内文書 Q&A から Web 閲覧を除く設計を挙げています。共有文書や転記したメールにも攻撃者の入力は入りうるため、この操作だけでは持ち出し経路を「構造的に閉じる」条件を満たしません。文書の保管場所と、内容の信頼性は別です。

修正方針: Web 無効化は攻撃面の縮小と説明し、入力の除去とするには書き手・取り込み経路・出所の信頼管理が必要とします。社内検索結果もデータとして扱い、命令や権限の根拠にしない条件を揃えます。

## 改善提案と保守上の課題

| ID | 根拠 | 提案 |
| --- | --- | --- |
| S01 | [website/content-src/index.mdx](website/content-src/index.mdx) L19〜20 | 「全ドキュメント」の本文目次が 10 章までで、11〜15 の 5 章・36 記事への入口がありません。サイドバーには存在するため到達不能ではありませんが、生成セクション一覧を使うか、手書き目次を同期します |
| S02 | 参考資料セクションの全件走査 | 外部 URL がない記事は 66 / 199、うちテンプレートを参考資料に挙げる記事は 15 件です。独自の方法論なら直ちに誤りではありません。性能・安全性・心理特性などの実証主張を含む記事では、テンプレートは根拠にならないため、主張に対応する一次資料を追加します |
| S03 | [MAINTENANCE-2026Q3-PLAN.md](MAINTENANCE-2026Q3-PLAN.md) の進捗表 | M1〜M3 は完了、M4〜M6 は未着手です。「全系統一括最新化」の完了とは区別して管理します。TODO の 7 月残を一律に期限切れ扱いせず、未実施系統と実装選定時確認型を分けて棚卸しします |
| S04 | [llm-landscape.md](docs/03-implementation/llm-landscape.md)、[openai-codex.md](docs/08-coding-agents/openai-codex.md) | 8 月時点のスナップショットとしての表記は明確ですが、9 月の公式モデル一覧は GPT-6 Astra を掲載し、Codex の旧モデルの期限も到来しています。意思決定に使うカタログから差分を反映します。[OpenAI モデル一覧](https://developers.openai.com/api/docs/models)、[Codex モデル](https://learn.chatgpt.com/docs/models)(確認日: 2026-09-10) |
| S05 | [poc-to-production.md](docs/09-business/poc-to-production.md) L78 | シャドーランの「利用者への影響ゼロ」に、副作用ツールのモック化・無効化という成立条件を追記します。[versioning-and-model-updates.md](docs/05-operations/versioning-and-model-updates.md) L63、L94 の説明と揃えます |
| S06 | [inference-internals.md](docs/11-llm-internals/inference-internals.md) L94〜97 | 投機的デコーディングの「一致したトークンを採用」だけでは、一般のサンプリングでの分布保存の説明が不足します。確率比による受理と棄却後の補正分布、貪欲法の場合を区別します。[原論文 Algorithm 1 / Appendix A.1](https://proceedings.mlr.press/v202/leviathan23a/leviathan23a.pdf)(確認日: 2026-09-10) |
| S07 | [privacy-enhancing-technologies.md](docs/06-security/privacy-enhancing-technologies.md) L47、L55 | 連合学習は生データを中央へ集めなくても、更新情報から漏えいしうることを補います。[NIST Privacy attacks in federated learning](https://www.nist.gov/blogs/cybersecurity-insights/privacy-attacks-federated-learning)(確認日: 2026-09-10) |
| S08 | [context-compaction-and-isolation.md](docs/02-architecture/context-compaction-and-isolation.md) L89〜91 | サブエージェントの要約も、非信頼の指示を運びえます。子の戻り値をデータとして扱う出力契約と、親の権限制約・承認境界を明示します |
| S09 | [.github/workflows/ci.yml](.github/workflows/ci.yml)、[check-links.mjs](scripts/check-links.mjs) L61〜69 | 現行チェックはサンプルの意味的な失敗やアンカー不在を検出しません。今回の修正に合わせ、誤採点・途中終了・MCPエラー分類・承認フロー・MDX 属性式・生成ページの skip link など、実害に対応する回帰チェックを追加します |
| S10 | [rag-implementation-patterns.md](docs/03-implementation/rag-implementation-patterns.md) L88 | 件数制限後の権限フィルタでは、上位候補がすべて権限外の場合、下位にある閲覧可能な文書を返せません。検索時の権限条件適用を基本候補とし、後フィルタを選ぶ場合は候補数拡張と権限制限下の再現率を評価します |

## 修正の進め方

1. F01 の監査ゲートを復旧し、F02 の承認図を修正します。
2. F03〜F04 の生成・編集ガードを直し、無害な異常入力で拒否を確認します。
3. F05〜F12、F23〜F26 の安全性・評価・設計の説明を直し、関連章を同期します。
4. F13〜F17 のサンプルを修正し、正常系と今回の失敗条件を Python 3.11 以降で確認します。
5. F18〜F22 と導線・参考資料を修正し、未実施の定点観測へ戻ります。

記事の実質修正時は `last_updated` を更新します。既存 published 記事の status を機械的に変更せず、修正後の意味的レビューと、全体 lint・validate・links・basePath 付き build を実施します。依存更新後の audit、実ブラウザーでの検索・スキップリンク・依存マップの受入確認は別途必要です。

## 代表的な再現方法

### 誤採点(F13、API キー不要)

`examples/python/evaluation-harness/` で次を実行します。期待結果を否定するだけの分類器が全件合格してしまいます。

```python
import eval_harness as h

class DenyingClassifier:
    def __init__(self, label):
        self.label = label

    def complete(self, prompt):
        return self.label + "ではありません。判定できません。"

cases = h.load_dataset()
passed = sum(
    h.judge(h.run_case(DenyingClassifier(c["expected"]), c), c["expected"])
    for c in cases
)
print(f"{passed}/{len(cases)}")  # 修正前: 5/5
```

### Codex ガードの入力不一致(F04、ファイルは変更しません)

リポジトリ直下で Node の ES module として次を実行します。

```javascript
import { spawnSync } from 'node:child_process'

const event = {
  hook_event_name: 'PreToolUse',
  tool_name: 'apply_patch',
  cwd: process.cwd(),
  tool_input: {
    command: '*** Begin Patch\n*** Add File: website/content/probe.mdx\n+probe\n*** End Patch'
  }
}
const result = spawnSync(process.execPath, ['.codex/hooks/guard-generated.mjs'], {
  input: JSON.stringify(event),
  encoding: 'utf8'
})
console.log({ status: result.status, stderr: result.stderr })
// 修正前: { status: 0, stderr: '' }
```

### 全体の機械チェック

```powershell
npm run check
```

サイトは `website/` で次を実行します。監査の終了コードは後続コマンドで上書きせず、個別に確認します。

```powershell
npm audit --audit-level=high
$env:STATIC_EXPORT = '1'
$env:NEXT_PUBLIC_BASE_PATH = '/ai-agent-library'
$env:NEXT_PUBLIC_SITE_URL = 'https://pero3dev.github.io/ai-agent-library'
npm run build:clean
```
