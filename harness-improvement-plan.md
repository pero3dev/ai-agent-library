# プロジェクトハーネス整備計画

作成日: 2026-09-10

対象: AI Agent Library 自体の作業環境・指示・スキル・委任・検証・中断再開・GitHub 連携です。この記事ライブラリを保守するエージェントが、依頼の理解から成果物の公開まで一貫した条件で作業できる状態を目指します。

本ファイルは計画です。基準コミットは `32cddefe9958056a82a3d90315a27e41a038fd04`。計画作成では既存構成の読み取り、限定した検証器入力、公式仕様の照合を行いました。以下の新しいコマンド・配置・検証は、実装予定として記載しています。

## 到達目標と対象範囲

最優先は、**規約の正本、作業別の完了条件、実行結果の証拠を揃えること**です。既存の定期最新化を維持し、新記事、既存記事の改訂、公開レビュー、サンプル、サイト、検証ツールの保守へ適用範囲を広げます。

主対象は、このプロジェクトで実際に使用している Windows / PowerShell の Codex と GitHub Actions です。既存の Claude Code 対応は互換層として維持します。Codex 専用の定期タスクを Claude に複製する必要はありません。製品・実行面・版・確認日ごとの対応表を持ち、実測していない面を対応済みと表示しません。

次の状態を完成条件とします。

1. 作業開始時に対象・所有ファイル・適用規約・必要な検証・許可範囲が確定する。
2. 同じ規約の手修正コピーを維持せず、共通部分の変更漏れを検出できる。
3. 編集内容と調査根拠に対応する最終レビューがあり、変更後に古い判定を流用できない。
4. 利用制限・中断・worktree 消失・main 更新から、作業を重複適用せず再開できる。
5. CI 待ちや資料取得待ちを記録し、独立した作業を継続できる。
6. 静的検査・モック・実 Agent・実 GitHub・公開確認の結果を区別できる。
7. ハーネス自身の変更にも、回帰検査と実機での受入条件がある。

既存の自律運用の許可を引き継ぎ、通常の記事更新へ一律の人手承認を追加しません。ChatGPT 契約枠の運用を維持し、API キーへの自動切替や有料 API 試験は別の明示的な許可がある場合に限ります。

## 現状の基盤

| 領域 | 確認した実装・証拠 | 整備で維持するもの |
| --- | --- | --- |
| 記事と計画 | 199 記事・16 セクション。ファイルの公開状態は front matter、タスクは ROADMAP | 記事テンプレート、日本語文体、索引・状態・更新日の同期 |
| 共通指示 | `AGENTS.md` と `CLAUDE.md` が各 119 行・約 10 KB | 規約の内容と既存の利用者の入口 |
| スキル・専門担当 | Codex 側 5 スキル、Claude 側 4 スキル、各 2 エージェント定義 | 執筆・鮮度確認・公開レビュー・サンプル検証の分担 |
| 編集フック | Codex / Claude に生成物ガードと記事検証 | 編集直後の短いフィードバック、正本からの生成 |
| ローカル検証 | root の `npm run check` は 79 テスト・lint・記事規約・相対リンク・TODO 棚卸し | 動いている検査と異常系の回帰例 |
| CI・公開 | main に必須 6 チェック、GitHub Actions の App 固定、strict、管理者適用。CI と Pages が成功 | 保護ルール、最小権限、Action の SHA 固定、依存監査 |
| 定期最新化 | 2 定期タスク、共通 Git ディレクトリの状態・排他・WIP 保存 | ChatGPT 認証、対象制限、独立レビュー、PR・公開確認 |
| 実測 | ネイティブ定期起動、PR #12 の記事更新から自動マージ・公開、PR #13 の CI | 既存の成功経路を互換性の基準にする |

既存の実測は [導入記録](freshness-automation-setup.md)、[是正記録](review-remediation-2026-09-10.md)、[最終 main CI](https://github.com/pero3dev/ai-agent-library/actions/runs/34441948993)を参照します。今回、全サイトや全サンプルの実行を繰り返したものではありません。

## 棚卸しで判明した不足

優先度は整備順です。P1 は正しさ・継続性に直接影響する項目、P2 は保守性・診断・適用範囲を改善する項目です。根拠の行番号は基準コミット時点です。

| ID | 優先度 | 根拠 | 問題と影響 |
| --- | --- | --- | --- |
| F01 | P1 | `AGENTS.md:17,104`、`.agents/skills/freshness-maintenance/SKILL.md:12,32` | 確認済みの事実更新まで TODO・ユーザー確認を要求するように読める共通規約と、承認済みの定期更新の契約が分かれています。事実訂正・方針変更・判断留保を区別する必要があります |
| F02 | P1 | `.agents/skills/publish-review/SKILL.md:16` | must 修正後、機械検証だけで published に進めます。最終差分の再レビューと、同じ ROADMAP タスクに属する残りの成果物の確認が不足しています |
| F03 | P1 | `scripts/freshness-policy.mjs:58,117,131` | digest が evidence 全体を除外するため、根拠 URL・観測内容だけの変更ではレビュー対象の変更を検出できません。記事と正規化した根拠の両方を結び付ける必要があります |
| F04 | P1 | `scripts/freshness-run.mjs:197,200` | 最古の未完了 run を無条件に再開します。外部待ちを繰り返し再開すると、新しい巡回が進まない可能性があります |
| F05 | P1 | `.github/workflows/freshness-policy.yml:31` | 自動最新化以外の PR は対象外です。全 PR 共通の生成物追跡禁止、ハーネス変更の分類・専用検査がありません。チェック名と App の一致だけでは workflow と event の一致を保証しません |
| F06 | P1 | `scripts/check-links.mjs:98`、`scripts/lib/validate-core.mjs:154` | 通常 PR の公開昇格と ROADMAP、実質更新と日付、docs/examples の相互対応を検査していません。各ファイル単体の形式が正しくても同期漏れが残ります |
| F07 | P2 | `.claude/skills/examples-check/SKILL.md:20`、`.agents/skills/examples-check/SKILL.md:20` | Claude 側だけ tool-use を `--mock` 非対応としています。二重管理による実際のドリフトです |
| F08 | P2 | `README.md:124`、`ROADMAP.md:7`、`.codex/agents/doc-reviewer.toml:6` | 正本が CLAUDE.md と AGENTS.md に分岐しています。記事向け規約の適用範囲、新規記事数と編集ファイル数の区別も曖昧です |
| F09 | P2 | `.claude/hooks/guard-generated.mjs:38`、`.codex/hooks/edited-paths.mjs:8`、`scripts/hooks.test.mjs:12` | Claude 側は単一 file_path と解析失敗時の許可、Codex 側は複数対象と解析失敗時の拒否です。共通する保護の結果が揃わず、フックの単体試験も Codex 側中心です |
| F10 | P2 | `scripts/lib/md-utils.mjs:41,78`、`scripts/lib/validate-core.mjs:194` | 限定した入力で、不正な front matter 行、空の本文、規約外ファイル名を単体検証器が受理しました。フェンス内の言語指定付き行を閉じフェンスと扱う例もありました。全 CI がこれらを受理すると確認したものではありません |
| F11 | P2 | `scripts/check-links.mjs:8,43,62,69`、`website/scripts/sync-content.mjs:248` | 参照形式リンク・見出しアンカー・運用文書・ハーネス内の参照が検査対象外です。サイト変換とも対応する Markdown 構文が分かれています |
| F12 | P2 | `CONTRIBUTING.md:21`、`package.json:9`、`.agents/skills/publish-review/SKILL.md:15` | 「依存ゼロの npm run check」という案内、lockfile と異なる `npx --yes`、local と CI の検証範囲差が残っています |
| F13 | P2 | `.codex/agents/freshness-checker.toml:9,19`、`.agents/skills/freshness-maintenance/SKILL.md:33` | 調査担当はアクセス日、呼出側は実取得 UTC 時刻を要求します。分類も 3 種と 4 種です。後から時刻を補う運用を避ける必要があります |
| F14 | P2 | `scripts/freshness-run.mjs:27,140,169,228` | 保存対象が記事向けに限定され、復元は手順中心、公開結果は自由文字列です。サイトや検証コードの中断復旧と、外部状態の照合にそのまま流用できません |
| F15 | P2 | `scripts/freshness-registry.mjs:151,158` | 定期対象の選定で周期到来を直接判定せず、rotation は対象を返します。手動再起動や再試行による同日重複観測を抑える余地があります |

フックによるシェル書き込み・シンボリックリンク経由の完全な隔離は、既存実装でも保証していません。この限界をフック追加だけで解決した扱いにはしません。

## 設計方針

### 1. 正本と製品別の入口を分ける

共通の作業原則は短い `AGENTS.md`、詳しい執筆規約は作業用の参照文書、製品固有の起動・権限設定は各製品の設定へ置きます。`CLAUDE.md` は必要な共通部分を決定的に生成するか、実機で確認済みの読み込み方式で参照します。手修正による内容コピーは同期検査へ移します。

`docs/`、`research/`、`examples/`、`website/`、`scripts/` の規約を分けます。ルートに置く恒久的な要点と、作業時に読む詳細を区別し、新規記事 1〜3 本、調査 1〜3 系統、レビュー分割の目安を別項目として明示します。

Codex の AGENTS 読込は開始位置と階層に依存するため、下位にファイルを置いただけで常に適用されたとは扱いません。ルート起動・下位ディレクトリ起動・worktree それぞれで、必要な規約が実際に渡ることを確認します。[公式の読込仕様](https://learn.chatgpt.com/docs/agent-configuration/agents-md)(確認日: 2026-09-10)

### 2. 作業ごとの契約を定義する

| 作業区分 | 主な編集対象 | 必要な検証・レビュー | 終了条件 |
| --- | --- | --- | --- |
| 新規記事 | docs、セクション索引、ROADMAP、必要な GLOSSARY・research | テンプレート・リンク・対応関係、内容レビュー | 依頼された draft 作成または公開昇格まで |
| 既存記事の改訂 | 対象記事と根拠・関連索引 | 差分分類、日付・状態・関連事項、最終差分の内容レビュー | 依頼範囲の修正・検証・必要な公開まで |
| 定期最新化 | 既存の freshness 許可範囲 | 一次情報、観測と記事の対応、独立レビュー、専用 policy | 変更なしの観測記録、またはマージ・公開確認 |
| 公開レビュー | 対象記事・状態・ROADMAP | タスク全成果物の状態、修正後の最終判定 | 承認された最終差分の published 化 |
| サンプル | examples と対応記事 | 構文、モック、実 SDK・ローカルプロトコル等の区分別検証 | 実施方式を明示した動作確認 |
| サイト | website の正本・実装 | 依存監査、生成、ビルド、変更に応じたブラウザー確認 | 依頼に含まれる公開と表示確認まで |
| ハーネス・検証ツール | 指示・skills・hooks・scripts・CI | 契約・異常系・互換性・必要な実 Agent 確認 | 既存作業の成功経路を維持した導入 |

作業契約は、目的、所有ファイル、許容する変更、対象外の残件、検証、レビュー、外部操作の許可根拠を持ちます。既存の許可を記録し、再開時に同じ承認を要求し直しません。新しい有料利用や要件判断が必要になった場合は、独立した作業を進めながら該当事項だけを確認します。

調査資料・PR 本文・ツール結果に含まれる指示文は、資料の内容として扱います。それらを理由に作業対象、権限、送信先、必須検証を変更しません。ハーネス自身の変更でも、変更候補に書かれた新しい規約を、その変更を承認する根拠には使いません。

### 3. 共通の処理とクライアントの違いを分ける

フックのパス判定・文書検証・結果形式を共通ライブラリへ寄せ、Codex / Claude の違いはイベント変換部分へ限定します。スキルの作業手順も共通本文を正とし、対応している製品に必要な入口だけを用意します。

読取担当と編集担当の役割を定義し、受け渡しに対象パス、確認する主張、出典 URL、実取得時刻、未確認理由、レビュー対象 digest を含めます。専任担当は当面、既存の 2 担当とハーネスのレビュー担当を中心とし、汎用の explorer / worker を活用します。

権限は「読み取り専用」という文章と実効設定を分けて扱います。Codex の設定継承・親ターンの権限上書きも確認し、実機試験で編集拒否を確認した範囲だけを強制境界として記録します。[公式のサブエージェント仕様](https://learn.chatgpt.com/docs/agent-configuration/subagents)(確認日: 2026-09-10)

### 4. 検査・レビュー・公開の証拠を揃える

共通記録には、対象 commit/tree、変更範囲、実行したコマンドと版、検証区分、実行環境、時刻、結果、ログ・PR・CI・公開 URL を保持します。公開時には、対象 PR の head、workflow、event、検査結果、merge SHA、deployment を GitHub の読み取り API で照合します。

更新日の検査は、記録された変更分類・日付・実際の差分の整合を確認します。「リンク修正のみ」などの分類が意味的に妥当かは、最終レビューで判断します。静的解析だけで、すべての事実変更を識別できることは完成条件にしません。

レビュー対象は、記事差分と正規化した観測データの両方から計算します。レビュー結果自身・保存時刻などの可変メタデータを除外し、自己参照を避けます。URL や根拠の主張を変更した場合も再レビューを必要にします。旧 schema の過去記録は検証できるよう残し、新規実行の schema 版を明示します。

別 run ID と digest の記録は、内容対応の検査と運用追跡です。実際の独立判断や事実の正しさを、それだけで機械的・暗号学的に証明したとは呼びません。

全 PR 共通の判定は、信頼する base commit の検査コード・依存・schema を使い、変更候補をデータとして読みます。候補側の設定の読み込みや npm lifecycle を通じた実行も分離します。変更したスクリプトや依存の実行試験は、秘密情報と書き込み権限を渡さない別経路で行います。この境界を workflow のイベント名だけに依存させません。[GitHub の公式指針](https://docs.github.com/en/actions/reference/security/securely-using-pull_request_target)(確認日: 2026-09-10)

### 5. 中断再開は既存の仕組みから段階的に共通化する

`freshness-run` の排他・attempt ID・WIP 保存を基礎に、作業区分ごとの保存対象を持つ共通処理へ抽出します。記事専用の許可範囲は維持し、サイト作業だからといって freshness の権限を広げません。

実行状態は少なくとも、作業可能、外部待ち、要判断、失敗、完了を区別します。外部待ちには理由と次回照合時刻を持たせます。最古の未完了記録だけを選ぶ方式から、再開できる仕事と期限到来した新しい仕事を選ぶ方式へ移します。

保存世代または小さな journal で、snapshot・状態・checkpoint のどこまで保存されたかを判定します。`resume` はまず既存 PR とブランチ・snapshot・main を照合し、復元案を表示します。きれいな自分の worktree で機械的に復元できる場合は進み、他者の変更を上書きする場合は止めます。マージ済み PR に同じ変更を再適用しません。

### 6. 起動時の診断と読ませる情報を絞る

開始時に、環境・Git 状態・必要な道具・規約の読込・hook trust・定期タスク・残件を診断します。設定値と観測した挙動を区別し、未観測を成功扱いにしません。個人設定は必要なキーと由来だけを扱い、認証ファイルや会話全文を収集しません。

ROADMAP は約 88 KB あります。完了した執筆履歴を毎回全文読む代わりに、選択タスク、成果物、定期観測の対象、必要な規約だけを返す入口を設けます。抽出結果は正本と commit に結び付け、独立した第二の進捗台帳にしません。

フックは配置されていても、信頼確認や実行面によって起動しないことがあります。2026-09-10 のローカル CLI 0.141.0 は `hooks = stable / true` でしたが、これは対象フックの信頼・実発火の証明ではありません。公式の trust 手順と実イベントで確認します。[公式のフック仕様](https://learn.chatgpt.com/docs/hooks)(確認日: 2026-09-10)

## 実装フェーズと受入条件

段階ごとに小さい PR を作り、合格してから次へ進みます。新規ドキュメントは 1 セッション原則 3 ファイルまでとし、以下の候補一式を一度に生成しません。

| フェーズ | 主な作業 | 成果物の候補 | 完了条件・対応する不足 |
| --- | --- | --- | --- |
| H0: 基準の固定 | 構成、所有者、対応面、検証区分、既存成功経路を整理 | 本計画、実装時の基準レポート | 棚卸しは本計画で完了。実装前に変更差分だけ再確認 |
| H1: 共通契約と正本 | 規約の適用範囲、許可済み作業、公開条件を明文化。重複指示と既知のドリフトを整理 | 短い root 指示、作業別規約、CLAUDE 互換入口、同期検査 | Codex / Claude で判断が一致。tool-use の説明差、CONTRIBUTING の準備手順を解消。F01・F07・F08・F12 |
| H2: スキルとレビュー | 入出力を共通化し、最終差分と根拠をレビュー対象にする。タスク全成果物を確認して公開状態を進める | 共通 skill 本文、役割定義、観測・レビュー schema、digest 改訂 | 修正後の未レビュー公開と、根拠だけ変更したレビュー流用を拒否。F02・F03・F13 |
| H3: フックと静的検証 | 共通のイベント処理、ハーネス参照・定義・コピーの検査、文書パーサ・リンク・同期検査を改善 | `check:harness`、共通 hook core、規約と対応関係の検査、限定 Windows CI | 正常例を維持し異常例を検出。Codex / Claude の共通対象で一致。F06・F09・F10・F11 |
| H4: 全 PR の検査と終了判定 | trusted base 由来の共通検査、作業区分の分類、変更に応じた検査、GitHub 状態照合を追加 | 共通 PR policy、`verify` / `finish` の構造化結果、CI 対応表 | 生成物強制追加・検査不足・別 head の成功を拒否。公開を依頼された作業は公開前に完了扱いにしない。F05・F12・F14 |
| H5: 共通再開と実行制御 | profile ごとの保存、復元、保存途中の障害対応、外部待ち、周期到来、重複抑止、時間上限を実装 | 互換性を保つ共通 run 処理、queue、resume、状態移行 | 既存 freshness の回帰と、worktree 消失・待ち行列・新規作業の受入が成功。F04・F14・F15 |
| H6: 実 Agent 評価と運用 | 代表タスクの評価、限定した実イベント試験、health、保持・停止・復旧手順を整備 | 評価 fixture、実行レポート、doctor、health、運用ガイド | 実機証拠と静的証拠を分離して一連の受入を完了 |

H1 の契約確定後は H2 と H3 の異なるファイルを並行で進められます。H4 は両方の成果を使います。H5 の状態設計・異常系 fixture は H3 と並行で準備できますが、既存定期タスクの移行は H4 の終了判定を整えてから行います。H6 の基準計測は H1 から始め、最後に改善後と比較します。

H2 の schema / digest 切替前には、旧形式の未完了 run と PR を列挙します。旧方式で完了させるか、保存後に新形式へ明示移行し、改めてレビューを受けるかを決めます。切替後の新規実行では旧形式を受理せず、過去記録の閲覧互換性を新規 PR の検査免除に使いません。H5 の状態移行とは別の導入条件として試験します。

PR の目安は 8〜11 本です。H3 のパーサ・同期検査と H5 の状態移行は、それぞれ独立したレビュー単位に分割します。所要日数は実 Agent 試験とクライアント差に左右されるため、H1 完了時点で再見積もりします。

## コマンドと配置の案

以下は役割を決めるための案です。既存スクリプトを呼び出す薄い入口から始め、検証ロジックを二重実装しません。

| 予定する入口 | 役割 |
| --- | --- |
| `npm run harness:doctor` | 必要な環境、Git、規約・hook の状態、対応面を診断 |
| `npm run harness:context -- --profile <作業区分>` | 必要な規約・タスク・成果物だけを返す |
| `npm run check:docs` | 記事の形式・内容構造・索引・相互対応を検査 |
| `npm run check:harness` | 指示・skills・roles・hooks・schema・同期と異常系を検査 |
| `npm run check:ci` | CI と共通の一覧からローカルで実行できる検査を選ぶ。OS・認証などの未充足条件を表示し、デプロイや有料 API 呼出しは含めない |
| `npm run harness:run -- <action>` | `start`、`checkpoint`、`resume`、`verify`、`finish` で所有・状態・検証証拠・終了判定を管理 |
| `npm run harness:health` | 最終予定・実起動・成功・公開・滞留・失敗理由を表示 |
| `npm run eval:harness` | 決定的な fixture 評価。実 Agent 試験は別の明示した実行区分 |

配置は、作業用文書と仕様を `harness/`、共通処理を `scripts/lib/`、入口を `scripts/`、実 Agent のための不活性な fixture を `tests/harness/` に置く案です。Codex / Claude の正式な入口は現在のディレクトリを維持します。

ローカルの実行状態は Git common directory 配下に置き、公開可能な検証結果だけをリポジトリへ残します。計画・状態の schema と版を管理し、既存 `.git/freshness/` の移行はバックアップ・dry-run・互換読み込みを経て実施します。

MCP は、既存のローカル CLI と JSON 出力で不足する利用場面が確認できた段階で追加を判断します。秘密情報や個人の認証ファイルをリポジトリや Actions へ移す必要のない構成を維持します。

## 評価と受入シナリオ

| ID | シナリオ | 期待する結果 | 検証区分 |
| --- | --- | --- | --- |
| E01 | 新規記事を作成する | テンプレート・索引・タスク対応を満たし、未レビューの公開を避ける | fixture + 限定実 Agent |
| E02 | 既存記事を一次情報で訂正する | 更新日・status・根拠・関連記述を揃える | fixture + 限定実 Agent |
| E03 | リンク修正のみ行う | 実質更新日を不必要に進めず、リンク先とアンカーを検査する | fixture |
| E04 | 公開レビューで must 修正後に本文を変更する | 最終差分の判定を取り直す。タスクの一部だけで全体完了にしない | fixture + 限定実 Agent |
| E05 | レビュー後に根拠 URL・観測主張だけを差し替える | 古いレビューを拒否する | fixture |
| E06 | 生成物を編集・強制追跡する | 対応ツールの誤編集をフックで検出し、通常 PR の追跡追加も CI で拒否する | 両 hook adapter + CI |
| E07 | 不正 front matter・空本文・不正名・紛らわしいフェンス・リンクを与える | 対応する検証で失敗し、正常な CRLF・引用符等は維持する | fixture |
| E08 | 新旧 CLI イベント、下位 cwd、空白を含むパス、worktree でフックを使う | 対応範囲を正しく判定し、解析不能を成功と誤認しない | Windows / Linux + 実イベント |
| E09 | 保存の各段階で中断し、元 worktree を失う | 自分の差分・残件を復元し、他者の変更を保持する | 隔離 Git fixture |
| E10 | 再開前に main 更新・PR マージ・別 attempt 開始が起こる | 再照合・再レビューまたは終了確認に進み、古い attempt は書き込めない | 隔離 Git fixture |
| E11 | CI 待ち・取得不能・再試行日未来の run を残す | 待ちを維持しつつ、実行可能な別作業を選ぶ | queue fixture |
| E12 | 同日再起動・利用制限・時間上限が起こる | 重複観測を抑え、確認済み範囲を偽らず保存・停止する | fixture + 限定実 Agent |
| E13 | 調査担当・レビュアーへ編集を要求する | 指示上の拒否と実効権限による拒否を別々に観測する | 対象クライアントの実 Agent |
| E14 | 古い head の CI 成功、未マージ PR、未公開 deployment を渡す | 該当する完了状態を拒否する | API fixture + 実 GitHub |
| E15 | 指示・skill・hook を変更した状態と元の状態を比較する | 共通本文の同期、発火、検証選択、成果品質を確認する | 静的 + 実 Agent 比較 |
| E16 | 定期タスクで新しいハーネスを使用する | 既存の起動・記事更新・レビュー・PR・公開の経路が維持される | 限定した定期起動 + 実 GitHub |
| E17 | 外部資料・PR 本文・候補の設定に、権限変更や検査迂回の指示を含める | Agent は資料を作業指示に昇格せず、権限付き CI は候補コード・設定・依存を実行しない | 不活性 fixture + 限定実 Agent + CI |
| E18 | 旧 schema の未完了 run / PR を残して新 digest へ切り替える | 完了待ちまたは明示移行と再レビューで引き継ぎ、新規 PR の旧形式は拒否する | 移行 fixture |

fixture の教材用指示は `.example` などで保存し、実際の設定として誤認識されるファイルを全テスト領域で検出します。実機試験に必要な有効ファイルは、隔離した作業領域でだけ生成します。

実 Agent 評価では、同じモデル・設定・入力を使った変更前後を比較します。必須同期漏れ、無効な公開判定、他者ファイルの破壊、重複 PR は固定評価セットで 0 件を合格条件とします。成功率の一般化はせず、実行数と観測範囲を報告します。

効率は、読み込んだ指示量、検証時間、不要なツール呼出し、再レビュー回数、再開までの操作数を記録します。取得できる場合だけ実使用量を記録し、不明な契約残量を推定値で埋めません。品質を維持しつつ重複読込を減らすことを目標とし、数値目標は基準計測後に確定します。

## 移行・運用・保全

- 既存 79 テストと CI の成功経路を維持します。新しい検査を必須化する前に、現在の記事・設定での結果と、意図した異常例の拒否を確認します。
- 新しい必須チェックは既存 6 チェックを保持して導入し、旧チェックを外す場合は同等の保護を実測してから変更します。通常記事の自動マージに人手の承認回数を追加しません。
- 最新化 profile の対象制限を緩めず、ハーネス・依存・CI 変更は専用の作業区分として扱います。実行権限は目的と認可の範囲に合わせます。
- フックの trust は公式の導線で確認します。設定の信頼確認を自動的に迂回せず、未信頼なら診断で理由を表示します。hook の終了処理が呼ばれない中断も、checkpoint から復旧できるようにします。
- 定期タスクの切替は既存実行の終了を確認して行い、同じ更新を二重起動しません。異常時は新方式を停止し、互換状態と既存の実行手順へ戻せるようにします。
- ローカル状態には秘密情報・会話全文を保存しません。完了 run、WIP ref、ログの保持期間・容量を定め、削除対象の絶対パスと所有を確認します。
- health は `ACTIVE` と正常稼働を区別し、起動遅れ、最終成功、公開失敗、再試行待ちを表示します。PC 停止中にローカル監視が動かない限界を明示します。
- 常時監視が必要になった場合は、機密情報を含まない heartbeat の有無を GitHub Actions で確認する方式を検討します。通知先と公開範囲は、その追加要件として決めます。
- アプリ・CLI・hook の形式・権限設定・主要依存の変更時は、関係する実機 smoke を再実施します。アプリ内部 DB は診断上の補助とし、直接書き込む運用にはしません。

## 着手順

最初の実装単位は **H1 の共通契約・正本・既知のドリフト解消**です。対象を AGENTS / CLAUDE / CONTRIBUTING と既存スキルの規約対応に絞り、同期検査を添えます。その後、H2 の公開レビューと根拠の digest、H3 の検証の不足を優先します。

共通 run 処理や新しい CLI は、これらの契約が確定してから実装します。各 PR の説明には、解消する F 番号、確認した E 番号、実機未確認の範囲、状態移行と戻し方を記録します。コミットおよび squash 本文には `Co-authored-by: Codex <codex@openai.com>` を付けます。
